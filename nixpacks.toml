[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt --no-cache-dir"]

[phases.build]
cmds = []

[start]
cmd = "streamlit run project.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
